/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { PeerAddress } from "#peer/PeerAddress.js";
import { ExchangeProvider, NewExchangeOptions } from "#protocol/ExchangeProvider.js";
import type { MessageExchange } from "#protocol/MessageExchange.js";
import { ChannelType, Duration } from "@matter/general";
import { Peer } from "./Peer.js";
import { PeerConnection } from "./PeerConnection.js";
/**
 * Produces {@link MessageExchange}s for a peer.
 */
export declare class PeerExchangeProvider extends ExchangeProvider {
    #private;
    constructor(peer: Peer, context: PeerConnection.Context);
    get maxPathsPerInvoke(): number;
    get peerAddress(): PeerAddress;
    /**
     * The transport type of the current session. Returns the actual transport (TCP/UDP) of the
     * newest active session, or UDP as default when no session is active.
     */
    get channelType(): ChannelType;
    connect(options?: NewExchangeOptions): Promise<void>;
    initiateExchange(options?: NewExchangeOptions): Promise<MessageExchange>;
    maximumPeerResponseTime(expectedProcessingTime?: Duration, includeMaximumSendingTime?: boolean): Duration;
}
//# sourceMappingURL=PeerExchangeProvider.d.ts.map