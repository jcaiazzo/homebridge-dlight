"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var Specifier_exports = {};
__export(Specifier_exports, {
  Specifier: () => Specifier,
  resolvePathForSpecifier: () => resolvePathForSpecifier
});
module.exports = __toCommonJS(Specifier_exports);
var import_general = require("@matter/general");
var import_model = require("@matter/model");
var import_types = require("@matter/types");
var import_MalformedRequestError = require("./MalformedRequestError.js");
/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
var Specifier;
((Specifier2) => {
  function clusterFor(specifier) {
    if (specifier === void 0) {
      throw new import_MalformedRequestError.MalformedRequestError(`Cannot designate command "${specifier}" without a cluster`);
    }
    if ("cluster" in specifier) {
      return specifier["cluster"];
    }
    return specifier;
  }
  Specifier2.clusterFor = clusterFor;
  function attributeFor(cluster, specifier) {
    if (typeof specifier === "object") {
      return specifier;
    }
    if (cluster === void 0) {
      const attr2 = import_types.Global.attributes[specifier];
      if (attr2 === void 0) {
        throw new import_MalformedRequestError.MalformedRequestError(`Cannot designate attribute "${specifier}" without a cluster`);
      }
      return attr2;
    }
    const attr = cluster.attributes?.[specifier] ?? import_types.Global.attributes[specifier];
    if (attr === void 0) {
      throw new import_MalformedRequestError.MalformedRequestError(`Cluster ${cluster.name} does not define attribute ${specifier}`);
    }
    return attr;
  }
  Specifier2.attributeFor = attributeFor;
  function commandFor(cluster, specifier) {
    if (typeof specifier !== "string") {
      return specifier;
    }
    if (cluster === void 0) {
      throw new import_MalformedRequestError.MalformedRequestError(`Cannot designate command "${specifier}" without a cluster`);
    }
    const command = cluster.commands?.[specifier];
    if (command === void 0) {
      throw new import_MalformedRequestError.MalformedRequestError(`Cluster ${cluster.name} does not define command ${specifier}`);
    }
    return command;
  }
  Specifier2.commandFor = commandFor;
  function eventFor(cluster, specifier) {
    if (typeof specifier === "object") {
      return specifier;
    }
    if (cluster === void 0) {
      throw new import_MalformedRequestError.MalformedRequestError(`Cannot designate event "${specifier}" without a cluster`);
    }
    const event = cluster.events?.[specifier];
    if (event === void 0) {
      throw new import_MalformedRequestError.MalformedRequestError(`Cluster ${cluster.name} does not define event ${specifier}`);
    }
    return event;
  }
  Specifier2.eventFor = eventFor;
  function clusterOf(request) {
    if (request.cluster) {
      return Specifier2.clusterFor(request.cluster);
    }
    return void 0;
  }
  Specifier2.clusterOf = clusterOf;
  function endpointIdOf(request) {
    if (typeof request.endpoint === "number") {
      return request.endpoint;
    }
    return request.endpoint?.number;
  }
  Specifier2.endpointIdOf = endpointIdOf;
})(Specifier || (Specifier = {}));
function resolvePathForSpecifier(location) {
  const endpointId = Specifier.endpointIdOf(location);
  const cluster = location.cluster ? Specifier.clusterFor(location.cluster) : void 0;
  let dmPath = new import_model.DataModelPath(endpointId ?? "*", "endpoint");
  dmPath = dmPath.at(cluster ? (0, import_general.camelize)(cluster.name) : "*", "cluster");
  if (location.attribute !== void 0) {
    const name = nameForElement(location.attribute, cluster && Specifier.attributeFor(cluster, location.attribute));
    dmPath = dmPath.at("state").at(name);
    if (location.listIndex === null) {
      dmPath = dmPath.at("[ADD]", "marker");
    }
    return dmPath;
  }
  if (location.event !== void 0) {
    const name = nameForElement(location.event, cluster && Specifier.eventFor(cluster, location.event));
    dmPath = dmPath.at("events").at(name);
    if (location.isUrgent) {
      dmPath = dmPath.at("!", "marker");
    }
    return dmPath;
  }
  if (location.command !== void 0) {
    const name = nameForElement(location.command, cluster && Specifier.commandFor(cluster, location.command));
    return dmPath.at(name);
  }
  return dmPath.at("*", "element");
  function nameForElement(specifier, resolved) {
    if (typeof specifier === "string") {
      return (0, import_general.camelize)(specifier);
    }
    if (resolved?.name !== void 0) {
      return (0, import_general.camelize)(resolved.name);
    }
    if (resolved?.id !== void 0) {
      return `0x${resolved.id.toString(16)}`;
    }
    return "?";
  }
}
//# sourceMappingURL=Specifier.js.map
