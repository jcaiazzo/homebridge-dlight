/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { Fabric } from "#fabric/Fabric.js";
import { MessageExchange } from "#protocol/MessageExchange.js";
import { NodeSession } from "#session/NodeSession.js";
import { SessionManager } from "#session/SessionManager.js";
import { Duration } from "@matter/general";
import { CaseAuthenticatedTag, NodeId } from "@matter/types";
export declare class CaseClient {
    #private;
    constructor(sessions: SessionManager);
    pair(exchange: MessageExchange, fabric: Fabric, peerNodeId: NodeId, options?: CaseClient.PairOptions): Promise<{
        session: NodeSession;
        resumed: boolean;
    }>;
}
export declare namespace CaseClient {
    interface PairOptions {
        expectedProcessingTime?: Duration;
        caseAuthenticatedTags?: readonly CaseAuthenticatedTag[];
        abort?: AbortSignal;
        maxInitialRetransmissions?: number;
        maxInitialRetransmissionTime?: Duration;
        initialRetransmissionTime?: Duration;
    }
}
//# sourceMappingURL=CaseClient.d.ts.map