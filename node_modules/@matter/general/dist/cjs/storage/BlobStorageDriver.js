"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var BlobStorageDriver_exports = {};
__export(BlobStorageDriver_exports, {
  BlobStorageDriver: () => BlobStorageDriver,
  FilesystemBlobStorageDriver: () => FilesystemBlobStorageDriver
});
module.exports = __toCommonJS(BlobStorageDriver_exports);
var import_MatterError = require("../MatterError.js");
var import_BaseStorageDriver = require("./BaseStorageDriver.js");
var import_DatafileRoot = require("./DatafileRoot.js");
/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
class BlobStorageDriver extends import_BaseStorageDriver.BaseStorageDriver {
  type = "blob";
}
class FilesystemBlobStorageDriver extends BlobStorageDriver {
  #root;
  #lock;
  constructor(namespace) {
    super();
    if (namespace !== void 0) {
      if (!(namespace instanceof import_DatafileRoot.DatafileRoot)) {
        throw new import_MatterError.ImplementationError("Filesystem blob storage driver requires a DatafileRoot namespace");
      }
      this.#root = namespace;
    }
  }
  get root() {
    return this.#root;
  }
  async initialize() {
    if (this.#lock) {
      throw new import_MatterError.ImplementationError("Filesystem blob storage driver is already initialized");
    }
    if (this.#root) {
      this.#lock = await this.#root.lock();
    }
  }
  async close() {
    await this.#lock?.close();
    this.#lock = void 0;
  }
}
//# sourceMappingURL=BlobStorageDriver.js.map
