"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var DnssdParameters_exports = {};
__export(DnssdParameters_exports, {
  DnssdParameters: () => DnssdParameters
});
module.exports = __toCommonJS(DnssdParameters_exports);
var import_Bytes = require("#util/Bytes.js");
/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
class DnssdParameters {
  #raw;
  constructor(raw) {
    this.#raw = raw;
  }
  get(key) {
    const v = this.#raw.get(key);
    return v === void 0 ? void 0 : import_Bytes.Bytes.toString(v);
  }
  /**
   * The original bytes for the given key, or `undefined` if absent.
   */
  raw(key) {
    return this.#raw.get(key);
  }
  has(key) {
    return this.#raw.has(key);
  }
  get size() {
    return this.#raw.size;
  }
  keys() {
    return this.#raw.keys();
  }
  *values() {
    for (const v of this.#raw.values()) {
      yield import_Bytes.Bytes.toString(v);
    }
  }
  *entries() {
    for (const [k, v] of this.#raw) {
      yield [k, import_Bytes.Bytes.toString(v)];
    }
  }
  [Symbol.iterator]() {
    return this.entries();
  }
  forEach(cb, thisArg) {
    for (const [k, v] of this.entries()) {
      cb.call(thisArg, v, k, this);
    }
  }
}
//# sourceMappingURL=DnssdParameters.js.map
