/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { Bytes, ChannelType, Lifetime, UdpSocket, UdpSocketOptions, UdpSocketType } from "@matter/general";
import * as dgram from "node:dgram";
export declare class NodeJsUdpSocket implements UdpSocket {
    #private;
    static create({ lifetime: lifetimeOwner, listeningPort, type, listeningAddress, netInterface, reuseAddress }: UdpSocketOptions): Promise<NodeJsUdpSocket>;
    readonly maxPayloadSize: number;
    constructor(lifetime: Lifetime, type: UdpSocketType, socket: dgram.Socket, netInterface?: string);
    addMembership(membershipAddress: string): void;
    dropMembership(membershipAddress: string): void;
    onData(listener: (netInterface: string | undefined, peerAddress: string, peerPort: number, data: Bytes) => void): {
        close: () => Promise<void>;
    };
    send(host: string, port: number, data: Bytes): Promise<void>;
    close(): Promise<void>;
    get port(): number;
    supports(type: ChannelType, address?: string): boolean;
}
//# sourceMappingURL=NodeJsUdpSocket.d.ts.map