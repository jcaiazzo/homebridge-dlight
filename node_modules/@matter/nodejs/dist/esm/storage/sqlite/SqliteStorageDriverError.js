/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { StorageError } from "@matter/general";
class SqliteStorageDriverError extends StorageError {
  constructor(methodType, contextKey, mainReason) {
    super(`[${methodType.toUpperCase()}] ${contextKey}: ${mainReason}`);
    this.methodType = methodType;
    this.contextKey = contextKey;
    this.mainReason = mainReason;
  }
  methodType;
  contextKey;
  mainReason;
}
export {
  SqliteStorageDriverError
};
//# sourceMappingURL=SqliteStorageDriverError.js.map
