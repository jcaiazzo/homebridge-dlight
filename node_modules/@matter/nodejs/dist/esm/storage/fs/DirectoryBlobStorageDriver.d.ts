/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { BlobStorageDriver, Bytes, type DataNamespace, FilesystemBlobStorageDriver } from "@matter/general";
/**
 * Filesystem-backed blob storage driver using nested directories.
 *
 * Contexts map to nested directories; keys map to files within those directories.
 * Context segments are percent-encoded to avoid path separator conflicts.
 */
export declare class DirectoryBlobStorageDriver extends FilesystemBlobStorageDriver {
    #private;
    static readonly id: string;
    static create(namespace: DataNamespace, _descriptor: BlobStorageDriver.Descriptor): DirectoryBlobStorageDriver;
    constructor(namespace: DataNamespace);
    get initialized(): boolean;
    initialize(): Promise<void>;
    close(): Promise<void>;
    openBlob(contexts: string[], key: string): Promise<Blob>;
    writeBlobFromStream(contexts: string[], key: string, stream: ReadableStream<Bytes>): Promise<void>;
    delete(contexts: string[], key: string): Promise<void>;
    has(contexts: string[], key: string): Promise<boolean>;
    keys(contexts: string[]): Promise<string[]>;
    contexts(contexts: string[]): Promise<string[]>;
    clearAll(contexts: string[]): Promise<void>;
}
//# sourceMappingURL=DirectoryBlobStorageDriver.d.ts.map