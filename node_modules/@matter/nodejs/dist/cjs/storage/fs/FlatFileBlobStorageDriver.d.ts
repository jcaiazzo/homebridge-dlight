/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { BlobStorageDriver, Bytes, type DataNamespace, FilesystemBlobStorageDriver } from "@matter/general";
/**
 * Blob storage driver using the legacy flat-file format from {@link FileStorageDriver}.
 *
 * All blobs live in a single directory.  The filename is the URI-encoded concatenation of context
 * segments and key joined by `.`.  Example: contexts `["bin", "fff1", "8000"]` with key `"prod"`
 * becomes the file `bin.fff1.8000.prod` (percent-encoded).
 */
export declare class FlatFileBlobStorageDriver extends FilesystemBlobStorageDriver {
    #private;
    static readonly id = "file";
    static create(namespace: DataNamespace, _descriptor: BlobStorageDriver.Descriptor): FlatFileBlobStorageDriver;
    constructor(namespace: DataNamespace);
    get initialized(): boolean;
    initialize(): Promise<void>;
    close(): Promise<void>;
    openBlob(contexts: string[], key: string): Promise<Blob>;
    writeBlobFromStream(contexts: string[], key: string, stream: ReadableStream<Bytes>): Promise<void>;
    delete(contexts: string[], key: string): Promise<void>;
    has(contexts: string[], key: string): Promise<boolean>;
    keys(contexts: string[]): Promise<string[]>;
    contexts(contexts: string[]): Promise<string[]>;
    clearAll(contexts: string[]): Promise<void>;
}
//# sourceMappingURL=FlatFileBlobStorageDriver.d.ts.map