/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { type DataNamespace, FilesystemStorageDriver, type StorageDriver, type SupportedStorageTypes } from "@matter/general";
export declare class JsonFileStorageDriver extends FilesystemStorageDriver {
    #private;
    static readonly id = "json";
    /** We store changes after a value was set to the storage, but not more often than this setting (in ms). */
    static commitDelay: import("@matter/general").Duration;
    committed: Promise<void>;
    constructor(namespaceOrPath?: DataNamespace | string);
    static create(namespace: DataNamespace, _descriptor?: StorageDriver.Descriptor): Promise<JsonFileStorageDriver>;
    get initialized(): boolean;
    initialize(): Promise<void>;
    get(contexts: string[], key: string): SupportedStorageTypes | undefined;
    set(contexts: string[], values: Record<string, SupportedStorageTypes>): void;
    set(contexts: string[], key: string, value: SupportedStorageTypes): void;
    delete(contexts: string[], key: string): void;
    keys(contexts: string[]): string[];
    values(contexts: string[]): Record<string, SupportedStorageTypes>;
    contexts(contexts: string[]): string[];
    clearAll(contexts: string[]): void;
    close(): Promise<void>;
}
//# sourceMappingURL=JsonFileStorageDriver.d.ts.map