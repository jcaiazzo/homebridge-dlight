"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var NodeJsTcpConnection_exports = {};
__export(NodeJsTcpConnection_exports, {
  NodeJsTcpConnection: () => NodeJsTcpConnection
});
module.exports = __toCommonJS(NodeJsTcpConnection_exports);
var import_general = require("@matter/general");
/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
const TCP_CLOSE_TIMEOUT = (0, import_general.Seconds)(5);
class NodeJsTcpConnection {
  remoteAddress;
  remotePort;
  localPort;
  #socket;
  #ended = false;
  /** Queued chunks waiting for the async iterator to consume. */
  #chunks = new Array();
  /** Resolver for a pending iterator next() call waiting for data. */
  #waiter;
  constructor(socket) {
    this.#socket = socket;
    socket.setNoDelay(true);
    socket.setKeepAlive(true, import_general.TCP_KEEP_ALIVE_INITIAL_DELAY_MS);
    this.remoteAddress = socket.remoteAddress ?? "";
    this.remotePort = socket.remotePort ?? 0;
    this.localPort = socket.localPort ?? 0;
    socket.on("data", (data) => {
      const chunk = new Uint8Array(data);
      if (this.#waiter) {
        const resolve = this.#waiter;
        this.#waiter = void 0;
        resolve({ value: chunk, done: false });
      } else {
        this.#chunks.push(chunk);
        if (this.#chunks.length > 1) {
          socket.pause();
        }
      }
    });
    socket.on("close", () => {
      this.#ended = true;
      this.#waiter?.({ value: void 0, done: true });
      this.#waiter = void 0;
    });
    socket.on("error", (_error) => {
      this.#ended = true;
      this.#waiter?.({ value: void 0, done: true });
      this.#waiter = void 0;
    });
  }
  [Symbol.asyncIterator]() {
    return {
      next: () => {
        if (this.#chunks.length > 0) {
          const chunk = this.#chunks.shift();
          if (this.#chunks.length === 0) {
            this.#socket.resume();
          }
          return Promise.resolve({ value: chunk, done: false });
        }
        if (this.#ended) {
          return Promise.resolve({ value: void 0, done: true });
        }
        this.#socket.resume();
        return new Promise((resolve) => {
          this.#waiter = resolve;
        });
      }
    };
  }
  send(data) {
    return new Promise((resolve, reject) => {
      this.#socket.write(import_general.Bytes.of(data), (error) => {
        if (error) {
          reject(new import_general.NetworkError(error.message));
        } else {
          resolve();
        }
      });
    });
  }
  onClose(listener) {
    this.#socket.on("close", listener);
    return {
      close: async () => {
        this.#socket.removeListener("close", listener);
      }
    };
  }
  onError(listener) {
    const handler = (error) => {
      listener(new import_general.NetworkError(error.message));
    };
    this.#socket.on("error", handler);
    return {
      close: async () => {
        this.#socket.removeListener("error", handler);
      }
    };
  }
  async close() {
    if (this.#socket.destroyed) {
      return;
    }
    this.#ended = true;
    this.#waiter?.({ value: void 0, done: true });
    this.#waiter = void 0;
    const closed = new Promise((resolve) => {
      this.#socket.once("close", () => resolve());
      this.#socket.end();
    });
    await (0, import_general.withTimeout)(TCP_CLOSE_TIMEOUT, closed, () => {
      this.#socket.destroy();
    });
  }
}
//# sourceMappingURL=NodeJsTcpConnection.js.map
