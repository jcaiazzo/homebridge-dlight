/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { Directory, File, type Filesystem, type FilesystemNode, type MaybePromise } from "@matter/general";
export declare class NodeJsDirectory extends Directory {
    #private;
    constructor(fs: Filesystem, path: string, name: string, cachedStat?: FilesystemNode.Stat);
    get fs(): Filesystem;
    get name(): string;
    get path(): string;
    exists(): Promise<boolean>;
    stat(): MaybePromise<FilesystemNode.Stat>;
    rename(newName: string): Promise<void>;
    delete(): Promise<void>;
    entries(): AsyncIterable<Directory.Entry>;
    file(name: string): File;
    directory(name: string): Directory;
    mkdir(): Promise<void>;
    copy(source: string | FilesystemNode, target: string | FilesystemNode): Promise<void>;
    lock(): Promise<() => Promise<void>>;
}
//# sourceMappingURL=NodeJsDirectory.d.ts.map