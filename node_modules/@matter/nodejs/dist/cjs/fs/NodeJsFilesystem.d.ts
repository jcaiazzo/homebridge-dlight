/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { Directory, File, Filesystem, type FilesystemNode } from "@matter/general";
/**
 * Filesystem backed by the local OS filesystem via Node.js APIs.
 */
export declare class NodeJsFilesystem extends Filesystem {
    #private;
    constructor(workingDirectory: string | (() => string));
    get name(): string;
    get path(): string;
    exists(): Promise<boolean>;
    stat(): Promise<FilesystemNode.Stat>;
    rename(): Promise<void>;
    delete(): Promise<void>;
    entries(): AsyncIterable<Directory.Entry>;
    file(name: string): File;
    directory(name: string): Directory;
    mkdir(): Promise<void>;
    copy(source: string | FilesystemNode, target: string | FilesystemNode): Promise<void>;
    tempFilename(): string;
    tempDirectory(): Directory;
}
//# sourceMappingURL=NodeJsFilesystem.d.ts.map