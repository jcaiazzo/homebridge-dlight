"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var ClassForValueModel_exports = {};
__export(ClassForValueModel_exports, {
  ClassForValueModel: () => ClassForValueModel
});
module.exports = __toCommonJS(ClassForValueModel_exports);
var import_general = require("@matter/general");
var import_model = require("@matter/model");
/**
 * @license
 * Copyright 2022-2026 Matter.js Authors
 * SPDX-License-Identifier: Apache-2.0
 */
const cache = /* @__PURE__ */ new WeakMap();
function ClassForValueModel(model) {
  let klass = cache.get(model);
  if (klass !== void 0) {
    return klass;
  }
  const metatype = model.effectiveMetatype;
  if (metatype !== import_model.Metatype.object && metatype !== import_model.Metatype.bitmap) {
    throw new Error(`ClassForValueModel only supports struct and bitmap metatypes, got ${metatype}`);
  }
  const scope = (0, import_model.Scope)(model);
  const defaults = (0, import_model.DefaultValue)(scope, model);
  klass = (0, import_general.GeneratedClass)({
    name: model.name,
    initialize(values) {
      if (defaults) {
        for (const key in defaults) {
          this[key] = defaults[key];
        }
      }
      if (values !== void 0) {
        if (typeof values === "number" && metatype === import_model.Metatype.bitmap) {
          const members = scope.membersOf(model, { conformance: "conformant" });
          for (const member of members) {
            const constraint = member.effectiveConstraint;
            if (typeof constraint.value === "number") {
              this[member.propertyName] = !!(values & 1 << constraint.value);
            } else if (typeof constraint.min === "number" && typeof constraint.max === "number") {
              const width = constraint.max - constraint.min;
              const mask = (1 << width) - 1;
              this[member.propertyName] = values >> constraint.min & mask;
            }
          }
        } else if (typeof values === "object") {
          for (const key in values) {
            this[key] = values[key];
          }
        }
      }
    }
  });
  import_model.Schema.set(klass, model);
  Object.freeze(klass);
  cache.set(model, klass);
  return klass;
}
//# sourceMappingURL=ClassForValueModel.js.map
